package com.yourgroup.airbattle.core;

/** Types used for collision rules and filtering. */
public enum ObjectType {
    PLAYER,
    ENEMY,
    BULLET_PLAYER,
    BULLET_ENEMY,
    POWERUP
}
